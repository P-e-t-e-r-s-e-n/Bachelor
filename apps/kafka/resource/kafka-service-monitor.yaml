apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: kafka-pod-monitor
  namespace: monitoring
  labels:
    release: stu245087-prometheus-minimal
spec:
  selector:
    matchLabels:
      strimzi.io/cluster: stu245087-kafka-cluster
      strimzi.io/kind: Kafka
      strimzi.io/name: stu245087-kafka-cluster-kafka-exporter
  namespaceSelector:
    matchNames:
      - kafka
  podMetricsEndpoints:
    - port: tcp-prometheus
      interval: 30s
      path: "/metrics"
