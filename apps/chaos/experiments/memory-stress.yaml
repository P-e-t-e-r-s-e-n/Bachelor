apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: kafka-memory-stress
  namespace: chaos
spec:
  mode: one
  selector:
    namespaces:
      - kafka
    labelSelectors:
      app: kafka
  stressors:
    memory:
      workers: 2
      size: "256"
  duration: "60s"