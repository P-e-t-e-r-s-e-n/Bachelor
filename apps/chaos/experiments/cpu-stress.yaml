apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: kafka-cpu-stress
  namespace: chaos
spec:
  mode: one
  selector:
    namespaces:
      - kafka
    labelSelectors:
      app: kafka
  stressors:
    cpu:
      workers: 2
      load: 80
  duration: "60s"